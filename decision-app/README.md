# decision-app

“帮我选”的 Vue 2 前端应用。完整的功能与数据规则说明见仓库根目录的 [`README.md`](../README.md)。

## 环境要求

- Node.js 18 或更高版本
- npm 8 或更高版本

## 开发

```bash
npm install
npm run serve
```

## 验证

```bash
npm run test:unit
npm run lint
npm run build
```

生产文件会输出到 `dist/`，该目录不提交到 Git。
